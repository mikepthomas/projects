type Experience = {
  title: string;
  startDate: string;
  endDate?: string;
  city: string;
  location: string;
  url?: string;
  description?: string;
  color: string;
  icon: string;
  inverted?: boolean;
};

export default Experience;
